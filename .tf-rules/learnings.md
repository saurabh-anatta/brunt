[chat] custom-nav-drawer desktop L1 is linklist-driven: the visible Shop/Learn/Rewards labels come from the native Horizon header menu (main-menu linklist), NOT from the nav-tab blocks. The drawer JS hooks header menu items whose text matches a configured nav-tab title and opens the drawer on hover. So main-menu MUST contain top-level links titled exactly like the tabs (no children, or Horizon's own mega-menu conflicts).
[chat] custom-nav-drawer L2 filters attach by handle: submenu-filter.parent_handle must match the handleized handle of an L1 menu item inside that tab's menu. Both the trigger button and filter-content panel are now handleized in liquid, so parent_handle can be entered as the readable title (e.g. "Boots") and it'll match. The filter_menu linklist structure: L1 with URL+no children=direct link; L1 with children=section group; L2 no grandchildren=tag pills; L2 with grandchildren=accordion.
[chat] custom-nav-drawer.js binds header hover hooks via MutationObserver (idempotent, data-cndHooked flag) because <header-menu> hydrates async — a one-shot bind on connectedCallback misses the links.
